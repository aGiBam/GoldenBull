export const environment = {
  production: true,
  // TODO: point this at your deployed API once the backend is hosted somewhere
  // (Railway/Render/Fly.io/VPS...). Kept separate from environment.ts on purpose
  // so a production build never accidentally calls localhost.
  apiUrl: 'https://api.goldenbull.example.com/api',
};
